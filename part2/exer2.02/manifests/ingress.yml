apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: exer-2-02-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: project-v1-0-svc
            port:
              number: 2353
